# Generated by XcodeGen on every CI run (and locally, if you ever get Mac access) — never
# commit it, project.yml is the actual source of truth.
ArenaMobile.xcodeproj/
Generated/

# Xcode / build junk, in case any of this is ever produced locally.
build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store

# Build artifacts you'll download from GitHub Actions — keep them out of the repo.
*.ipa
Payload/
